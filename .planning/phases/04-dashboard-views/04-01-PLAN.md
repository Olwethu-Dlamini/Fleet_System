---
phase: 04-dashboard-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vehicle-scheduling-backend/src/controllers/dashboardController.js
  - vehicle-scheduling-backend/src/routes/dashboard.js
  - vehicle_scheduling_app/pubspec.yaml
  - vehicle_scheduling_app/lib/config/app_config.dart
autonomous: true
requirements: [DASH-01, DASH-04]
must_haves:
  truths:
    - "GET /api/dashboard/chart-data returns hourly job counts for today scoped to tenant"
    - "All three dashboard routes require verifyToken authentication"
    - "All dashboard queries include tenant_id scoping"
    - "fl_chart dependency is available in the Flutter project"
  artifacts:
    - path: "vehicle-scheduling-backend/src/controllers/dashboardController.js"
      provides: "getChartData method with tenant-scoped hourly query"
      contains: "getChartData"
    - path: "vehicle-scheduling-backend/src/routes/dashboard.js"
      provides: "chart-data route with verifyToken on all routes"
      contains: "verifyToken"
    - path: "vehicle_scheduling_app/pubspec.yaml"
      provides: "fl_chart dependency"
      contains: "fl_chart"
    - path: "vehicle_scheduling_app/lib/config/app_config.dart"
      provides: "dashboardChartEndpoint getter"
      contains: "dashboardChartEndpoint"
  key_links:
    - from: "vehicle-scheduling-backend/src/routes/dashboard.js"
      to: "vehicle-scheduling-backend/src/controllers/dashboardController.js"
      via: "router.get /chart-data"
      pattern: "chart-data.*getChartData"
---

<objective>
Backend chart-data endpoint, dashboard security fixes, and Flutter dependency setup for Phase 4 dashboard enhancements.

Purpose: Provide the hourly job count API needed for the "Jobs Today" bar chart (DASH-01), fix the security gap on existing dashboard routes (verifyToken + tenant_id scoping), and add fl_chart dependency to Flutter.
Output: Working backend endpoint returning hourly job counts, secured dashboard routes, fl_chart available in Flutter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-views/04-RESEARCH.md

@vehicle-scheduling-backend/src/controllers/dashboardController.js
@vehicle-scheduling-backend/src/routes/dashboard.js
@vehicle_scheduling_app/pubspec.yaml
@vehicle_scheduling_app/lib/config/app_config.dart

<interfaces>
From vehicle-scheduling-backend/src/controllers/dashboardController.js:
```javascript
class DashboardController {
  static async getDashboardSummary(req, res) { ... }
  static async getQuickStats(req, res) { ... }
}
// NOTE: Neither method uses req.user.tenant_id — security gap to fix
```

From vehicle-scheduling-backend/src/routes/dashboard.js:
```javascript
router.get('/summary', dashboardController.getDashboardSummary);
router.get('/stats', dashboardController.getQuickStats);
// NOTE: No verifyToken middleware on either route — security gap to fix
```

From vehicle-scheduling-backend/src/middleware/authMiddleware.js:
```javascript
const { verifyToken } = require('../middleware/authMiddleware');
// verifyToken sets req.user with { id, tenant_id, role, ... }
```

From vehicle_scheduling_app/lib/config/app_config.dart:
```dart
static const String dashboardEndpoint = '/dashboard';
// Need to add: static String get dashboardChartEndpoint => '$dashboardEndpoint/chart-data';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chart-data endpoint and fix dashboard route security</name>
  <files>
    vehicle-scheduling-backend/src/controllers/dashboardController.js,
    vehicle-scheduling-backend/src/routes/dashboard.js
  </files>
  <read_first>
    vehicle-scheduling-backend/src/controllers/dashboardController.js,
    vehicle-scheduling-backend/src/routes/dashboard.js,
    vehicle-scheduling-backend/src/middleware/authMiddleware.js
  </read_first>
  <action>
    1. In `dashboardController.js`, add a new static method `getChartData(req, res)`:
       - Extract `tenantId` from `req.user.tenant_id` (set by verifyToken middleware)
       - Get today's date as `YYYY-MM-DD` string
       - Query: `SELECT HOUR(scheduled_time_start) AS hour, COUNT(*) AS count FROM jobs WHERE scheduled_date = ? AND tenant_id = ? AND current_status NOT IN ('cancelled') GROUP BY HOUR(scheduled_time_start) ORDER BY hour ASC`
       - Return JSON: `{ success: true, date: today, hourly: [{ hour: N, count: N }, ...] }`
       - Wrap in try/catch, log errors with `log.error({ err }, 'getChartData error')`
       - Return 500 on error with `{ success: false, error: err.message }`

    2. Fix `getDashboardSummary` — add `AND tenant_id = ?` to ALL queries that touch `jobs`, `job_status_changes`, `vehicles`, `job_assignments` tables. Use `req.user.tenant_id` as the parameter. The 5 parallel queries all need this fix.

    3. Fix `getQuickStats` — add `AND tenant_id = ?` to both the all-time and today's-count queries. Use `req.user.tenant_id`.

    4. In `dashboard.js` routes file:
       - Add `const { verifyToken } = require('../middleware/authMiddleware');` at the top
       - Add `verifyToken` middleware to ALL three routes:
         - `router.get('/summary', verifyToken, dashboardController.getDashboardSummary);`
         - `router.get('/stats', verifyToken, dashboardController.getQuickStats);`
         - `router.get('/chart-data', verifyToken, dashboardController.getChartData);`
  </action>
  <verify>
    <automated>cd vehicle-scheduling-backend && node -e "const c = require('./src/controllers/dashboardController'); console.log(typeof c.getChartData === 'function' ? 'PASS: getChartData exists' : 'FAIL')" && node -e "const fs = require('fs'); const r = fs.readFileSync('src/routes/dashboard.js','utf8'); const checks = [r.includes('verifyToken'), r.includes('chart-data'), r.includes('getChartData')]; console.log(checks.every(Boolean) ? 'PASS: routes wired' : 'FAIL: ' + checks)"</automated>
  </verify>
  <acceptance_criteria>
    - dashboardController.js contains static async getChartData method
    - getChartData query includes WHERE tenant_id = ?
    - getDashboardSummary queries all include tenant_id = ?
    - getQuickStats queries all include tenant_id = ?
    - dashboard.js imports verifyToken from authMiddleware
    - All three routes (/summary, /stats, /chart-data) have verifyToken middleware
  </acceptance_criteria>
  <done>All dashboard endpoints are auth-protected and tenant-scoped; chart-data endpoint returns hourly job counts</done>
</task>

<task type="auto">
  <name>Task 2: Add fl_chart dependency and AppConfig endpoint</name>
  <files>
    vehicle_scheduling_app/pubspec.yaml,
    vehicle_scheduling_app/lib/config/app_config.dart
  </files>
  <read_first>
    vehicle_scheduling_app/pubspec.yaml,
    vehicle_scheduling_app/lib/config/app_config.dart
  </read_first>
  <action>
    1. In `pubspec.yaml`, add `fl_chart: ^1.2.0` under the `dependencies:` section (after the existing dependencies like `provider`, `http`, etc.).

    2. Run `cd vehicle_scheduling_app && flutter pub get` to resolve the dependency.

    3. In `app_config.dart`, add a new static getter after the existing `dashboardEndpoint` constant:
       ```dart
       // GET /api/dashboard/chart-data
       static String get dashboardChartEndpoint => '$dashboardEndpoint/chart-data';
       ```

    4. Verify flutter analyze passes with no errors in app_config.dart.
  </action>
  <verify>
    <automated>cd vehicle_scheduling_app && flutter pub get && flutter analyze lib/config/app_config.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains "fl_chart:" dependency
    - flutter pub get succeeds without errors
    - app_config.dart contains dashboardChartEndpoint getter
    - flutter analyze shows no errors on app_config.dart
  </acceptance_criteria>
  <done>fl_chart is installed and importable; AppConfig has the chart-data endpoint path</done>
</task>

</tasks>

<verification>
- `node -e "require('./vehicle-scheduling-backend/src/controllers/dashboardController').getChartData"` loads without error
- `grep -c "verifyToken" vehicle-scheduling-backend/src/routes/dashboard.js` returns 4 (1 import + 3 route uses)
- `grep "tenant_id" vehicle-scheduling-backend/src/controllers/dashboardController.js | wc -l` returns 5+ (one per query)
- `grep "fl_chart" vehicle_scheduling_app/pubspec.yaml` returns a match
- `grep "dashboardChartEndpoint" vehicle_scheduling_app/lib/config/app_config.dart` returns a match
</verification>

<success_criteria>
- Backend chart-data endpoint exists and returns hourly job counts scoped to tenant
- All dashboard routes are protected by verifyToken middleware
- All dashboard queries filter by tenant_id
- fl_chart dependency is in pubspec.yaml and resolved
- AppConfig has dashboardChartEndpoint getter
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-views/04-01-SUMMARY.md`
</output>
