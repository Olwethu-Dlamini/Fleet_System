---
phase: 04-dashboard-views
plan: 03
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - vehicle-scheduling-backend/src/models/Job.js
  - vehicle-scheduling-backend/src/controllers/dashboardController.js
autonomous: true
gap_closure: true
requirements: ["DASH-01"]

must_haves:
  truths:
    - "Job.getJobsByDate() accepts and applies tenant_id to its SQL WHERE clause"
    - "getDashboardSummary todayJobs field returns only jobs belonging to the authenticated tenant"
  artifacts:
    - path: "vehicle-scheduling-backend/src/models/Job.js"
      provides: "Tenant-scoped getJobsByDate query"
      contains: "j.tenant_id = ?"
    - path: "vehicle-scheduling-backend/src/controllers/dashboardController.js"
      provides: "Passes tenantId to getJobsByDate"
      contains: "Job.getJobsByDate(today, null, tenantId)"
  key_links:
    - from: "vehicle-scheduling-backend/src/controllers/dashboardController.js"
      to: "vehicle-scheduling-backend/src/models/Job.js"
      via: "Job.getJobsByDate(today, null, tenantId)"
      pattern: "getJobsByDate\\(today,\\s*null,\\s*tenantId\\)"
---

<objective>
Fix multi-tenant data leak in getDashboardSummary's todayJobs query.

Purpose: Job.getJobsByDate() has no tenant_id parameter, causing the /dashboard/summary endpoint to return jobs from ALL tenants for the todayJobs field. This is a blocker-severity security gap identified in 04-VERIFICATION.md.

Output: Tenant-scoped getJobsByDate method and patched getDashboardSummary call.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-views/04-01-SUMMARY.md

<interfaces>
<!-- Current method signature that needs tenant_id parameter -->

From vehicle-scheduling-backend/src/models/Job.js (line 317):
```javascript
static async getJobsByDate(date, statusFilter = null) {
  // SQL: WHERE j.scheduled_date = ?
  // No tenant_id in WHERE clause
}
```

From vehicle-scheduling-backend/src/controllers/dashboardController.js (line 66):
```javascript
// Inside getDashboardSummary, Promise.all block:
Job.getJobsByDate(today),
// tenantId is already available at line 46: const tenantId = req.user.tenant_id;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant_id scoping to Job.getJobsByDate and wire dashboardController call</name>
  <files>vehicle-scheduling-backend/src/models/Job.js, vehicle-scheduling-backend/src/controllers/dashboardController.js</files>
  <read_first>
    - vehicle-scheduling-backend/src/models/Job.js (lines 310-374 — getJobsByDate method)
    - vehicle-scheduling-backend/src/controllers/dashboardController.js (lines 40-70 — getDashboardSummary, specifically the Promise.all block)
  </read_first>
  <action>
    1. In `vehicle-scheduling-backend/src/models/Job.js`, modify the `getJobsByDate` method signature at line 317:
       - Change: `static async getJobsByDate(date, statusFilter = null)`
       - To: `static async getJobsByDate(date, statusFilter = null, tenantId = null)`

    2. In the same method, after the existing `WHERE j.scheduled_date = ?` clause (line 352) and before the statusFilter conditional (line 358), add tenant_id scoping:
       ```javascript
       // Add tenant scoping if provided
       if (tenantId) {
         sql += ' AND j.tenant_id = ?';
         params.push(tenantId);
       }
       ```

    3. In `vehicle-scheduling-backend/src/controllers/dashboardController.js` at line 66, change:
       - From: `Job.getJobsByDate(today),`
       - To: `Job.getJobsByDate(today, null, tenantId),`

       The `tenantId` variable is already declared at line 46 as `const tenantId = req.user.tenant_id;`.

    Do NOT modify the dashboardService.js calls — that file is dead code (no route imports it). Do NOT modify getJobsByDateRange — that is a separate method used by dashboardService only.
  </action>
  <verify>
    <automated>cd C:/Users/olwethu/desktop/test/vehicle-scheduling-backend && grep -n "tenant_id" src/models/Job.js | grep -i "getJobsByDate" && grep -n "getJobsByDate(today, null, tenantId)" src/controllers/dashboardController.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "tenantId = null" vehicle-scheduling-backend/src/models/Job.js returns the updated method signature on line 317
    - grep "j.tenant_id = ?" vehicle-scheduling-backend/src/models/Job.js returns a match inside the getJobsByDate method block (lines 317-374)
    - grep "getJobsByDate(today, null, tenantId)" vehicle-scheduling-backend/src/controllers/dashboardController.js returns exactly 1 match at line 66
    - No other lines in Job.js outside the getJobsByDate method are modified
    - No other files are modified
  </acceptance_criteria>
  <done>Job.getJobsByDate() accepts tenantId as third parameter and adds AND j.tenant_id = ? when provided. getDashboardSummary passes req.user.tenant_id so todayJobs only contains the authenticated tenant's jobs.</done>
</task>

</tasks>

<verification>
1. `grep -n "tenantId = null" vehicle-scheduling-backend/src/models/Job.js` — confirms new parameter in signature
2. `grep -n "j.tenant_id = ?" vehicle-scheduling-backend/src/models/Job.js` — confirms WHERE clause addition
3. `grep -n "getJobsByDate(today, null, tenantId)" vehicle-scheduling-backend/src/controllers/dashboardController.js` — confirms controller passes tenant_id
4. `cd vehicle-scheduling-backend && node -e "require('./src/models/Job')"` — confirms no syntax errors in Job.js
</verification>

<success_criteria>
The getDashboardSummary endpoint's todayJobs field is tenant-scoped. Job.getJobsByDate() includes AND j.tenant_id = ? when a tenantId argument is provided. The fix is backward-compatible (tenantId defaults to null, so existing callers without tenant_id continue to work).
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-views/04-03-SUMMARY.md`
</output>
