---
phase: 08-testing-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vehicle-scheduling-backend/tests/api/helpers/auth.js
  - vehicle-scheduling-backend/tests/api/helpers/db.mock.js
  - vehicle-scheduling-backend/tests/api/auth.test.js
  - vehicle-scheduling-backend/tests/api/jobs.test.js
  - vehicle-scheduling-backend/tests/api/vehicles.test.js
  - vehicle-scheduling-backend/tests/api/users.test.js
  - vehicle-scheduling-backend/tests/api/dashboard.test.js
  - vehicle-scheduling-backend/tests/api/reports.test.js
  - vehicle-scheduling-backend/tests/api/notifications.test.js
  - vehicle-scheduling-backend/tests/api/settings.test.js
  - vehicle-scheduling-backend/tests/api/job-assignments.test.js
  - vehicle-scheduling-backend/tests/api/job-status.test.js
  - vehicle-scheduling-backend/tests/api/availability.test.js
  - vehicle-scheduling-backend/tests/api/vehicle-maintenance.test.js
  - vehicle-scheduling-backend/tests/api/time-extensions.test.js
  - vehicle-scheduling-backend/tests/api/gps.test.js
  - vehicle-scheduling-backend/package.json
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Every backend API route has at least one happy-path test (200/201) and one auth-failure test (401)"
    - "All API tests run without a live MySQL database (DB is mocked)"
    - "npm run test:api executes all API tests and passes"
  artifacts:
    - path: "vehicle-scheduling-backend/tests/api/helpers/auth.js"
      provides: "JWT test fixture helper — makeToken(role, overrides)"
      exports: ["makeToken", "JWT_SECRET"]
    - path: "vehicle-scheduling-backend/tests/api/helpers/db.mock.js"
      provides: "jest.mock for database module"
      exports: ["db", "resetDbMocks"]
    - path: "vehicle-scheduling-backend/tests/api/auth.test.js"
      provides: "Auth route tests"
    - path: "vehicle-scheduling-backend/tests/api/jobs.test.js"
      provides: "Jobs route tests"
    - path: "vehicle-scheduling-backend/tests/api/vehicles.test.js"
      provides: "Vehicles route tests"
    - path: "vehicle-scheduling-backend/tests/api/gps.test.js"
      provides: "GPS route tests"
  key_links:
    - from: "tests/api/*.test.js"
      to: "tests/api/helpers/auth.js"
      via: "require('./helpers/auth')"
      pattern: "require.*helpers/auth"
    - from: "tests/api/*.test.js"
      to: "src/server"
      via: "require('../../src/server')"
      pattern: "require.*src/server"
---

<objective>
Create comprehensive API endpoint tests for all 15 backend routes using Jest + Supertest with mocked DB.

Purpose: Satisfy TEST-01 — every route gets at least one happy-path and one error test, runnable in CI without a database.
Output: 14 test files in tests/api/ (auth routes tested separately), 2 helper files, updated package.json scripts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-testing-suite/08-RESEARCH.md

<interfaces>
<!-- Route registrations from src/routes/index.js -->
router.use('/auth',            authRoutes);           // /api/auth/login
router.use('/jobs',            jobRoutes);             // /api/jobs
router.use('/vehicles',        vehicleRoutes);         // /api/vehicles
router.use('/job-assignments', jobAssignmentRoutes);   // /api/job-assignments
router.use('/job-status',      jobStatusRoutes);       // /api/job-status
router.use('/dashboard',       dashboardRoutes);       // /api/dashboard
router.use('/reports',         reportsRoutes);         // /api/reports
router.use('/users',           userRoutes);            // /api/users
router.use('/availability',    availabilityRoutes);    // /api/availability
router.use('/vehicle-maintenance', vehicleMaintenanceRoutes); // /api/vehicle-maintenance
router.use('/settings',        settingsRoutes);        // /api/settings
router.use('/notifications',   notificationRoutes);    // /api/notifications
router.use('/time-extensions', timeExtensionRoutes);   // /api/time-extensions
router.use('/gps',             gpsRoutes);             // /api/gps

<!-- Roles from src/config/constants.js -->
USER_ROLE = { ADMIN: 'admin', DISPATCHER: 'dispatcher', SCHEDULER: 'scheduler', TECHNICIAN: 'technician' }

<!-- server.js exports app, has require.main === module guard, JWT_SECRET process.exit(1) guard -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test helpers and first 7 route test files</name>
  <files>
    vehicle-scheduling-backend/tests/api/helpers/auth.js,
    vehicle-scheduling-backend/tests/api/helpers/db.mock.js,
    vehicle-scheduling-backend/tests/api/auth.test.js,
    vehicle-scheduling-backend/tests/api/jobs.test.js,
    vehicle-scheduling-backend/tests/api/vehicles.test.js,
    vehicle-scheduling-backend/tests/api/users.test.js,
    vehicle-scheduling-backend/tests/api/dashboard.test.js,
    vehicle-scheduling-backend/tests/api/reports.test.js,
    vehicle-scheduling-backend/tests/api/notifications.test.js,
    vehicle-scheduling-backend/package.json
  </files>
  <read_first>
    vehicle-scheduling-backend/src/server.js,
    vehicle-scheduling-backend/src/routes/index.js,
    vehicle-scheduling-backend/src/routes/authRoutes.js,
    vehicle-scheduling-backend/src/routes/jobs.js,
    vehicle-scheduling-backend/src/routes/vehicles.js,
    vehicle-scheduling-backend/src/routes/users.js,
    vehicle-scheduling-backend/src/routes/dashboard.js,
    vehicle-scheduling-backend/src/routes/reports.js,
    vehicle-scheduling-backend/src/routes/notifications.js,
    vehicle-scheduling-backend/src/config/constants.js,
    vehicle-scheduling-backend/src/config/database.js,
    vehicle-scheduling-backend/src/middleware/auth.js,
    vehicle-scheduling-backend/package.json,
    vehicle-scheduling-backend/tests/integration/securityHeaders.test.js
  </read_first>
  <action>
    1. Create `tests/api/helpers/auth.js`:
       - Export `JWT_SECRET = 'test-secret-value-minimum-32-chars-ok'`
       - Export `makeToken(role = 'admin', overrides = {})` that uses `jsonwebtoken.sign()` to mint a valid JWT with payload: `{ id: 1, username: 'test_' + role, role, tenant_id: 1, ...overrides }`
       - Token expiry: 1 hour

    2. Create `tests/api/helpers/db.mock.js`:
       - `jest.mock('../../src/config/database', ...)` providing mock `query`, `getConnection` (returns mock connection with `query`, `beginTransaction`, `commit`, `rollback`, `release`)
       - Export `db` (the mocked module) and `resetDbMocks()` function
       - IMPORTANT: The mock path must resolve correctly from tests/api/ to src/config/database. Read the actual database.js to confirm the export shape (pool.query vs db.query).

    3. Create test files for the first 7 routes. Each file MUST:
       - Set `process.env.JWT_SECRET` and `process.env.NODE_ENV = 'test'` BEFORE any require
       - Call `require('./helpers/db.mock')` to activate DB mock BEFORE requiring server
       - Import app via `require('../../src/server')`
       - Import `makeToken` from `./helpers/auth`
       - Test: 401 without token on protected routes
       - Test: 200/201 with valid admin token and mocked DB response for happy path
       - Call `resetDbMocks()` in `beforeEach`

       Route-specific tests:
       a) `auth.test.js`: POST /api/auth/login — test 400 with missing fields, test with mocked bcrypt/DB for valid login
       b) `jobs.test.js`: GET /api/jobs (admin 200, no-token 401), POST /api/jobs (admin with body, technician 403)
       c) `vehicles.test.js`: GET /api/vehicles (admin 200, no-token 401), POST /api/vehicles (admin OK, scheduler 403)
       d) `users.test.js`: GET /api/users (admin 200, technician 403), POST /api/users (admin OK, scheduler 403)
       e) `dashboard.test.js`: GET /api/dashboard/summary (admin 200, no-token 401)
       f) `reports.test.js`: GET /api/reports (admin 200, technician 403)
       g) `notifications.test.js`: GET /api/notifications (admin 200, no-token 401)

    4. Update `package.json` scripts — add:
       - `"test:api": "jest --testPathPattern=tests/api --forceExit"`
       - `"test:regression": "jest --testPathPattern=tests/regression --forceExit"`
       - `"test:all": "jest --forceExit"`
       Keep existing scripts unchanged. Use --forceExit to handle Socket.IO cleanup.

    IMPORTANT: Read each route file before writing its test to understand the exact endpoints, HTTP methods, and middleware (verifyToken, requirePermission). Do NOT guess endpoint paths — extract them from the route source.

    PITFALL: process.env.JWT_SECRET MUST be set at line 1 of every test file, before any require statements, or server.js will call process.exit(1) and kill Jest.

    PITFALL: The DB mock must match the actual export shape of src/config/database.js. Read it first. If it exports `pool` directly, mock accordingly. If it exports `{ query, getConnection }`, mock that shape.
  </action>
  <verify>
    <automated>cd vehicle-scheduling-backend && npm run test:api 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/helpers/auth.js exists and exports makeToken and JWT_SECRET
    - tests/api/helpers/db.mock.js exists and exports db and resetDbMocks
    - tests/api/auth.test.js exists with at least 2 test cases
    - tests/api/jobs.test.js exists with at least 3 test cases
    - tests/api/vehicles.test.js exists with at least 3 test cases
    - tests/api/users.test.js exists with at least 3 test cases
    - tests/api/dashboard.test.js exists with at least 2 test cases
    - tests/api/reports.test.js exists with at least 2 test cases
    - tests/api/notifications.test.js exists with at least 2 test cases
    - package.json contains "test:api" script
    - npm run test:api passes with 0 failures
  </acceptance_criteria>
  <done>7 route test files + 2 helpers created, all passing. package.json updated with test:api script.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining 8 route test files</name>
  <files>
    vehicle-scheduling-backend/tests/api/settings.test.js,
    vehicle-scheduling-backend/tests/api/job-assignments.test.js,
    vehicle-scheduling-backend/tests/api/job-status.test.js,
    vehicle-scheduling-backend/tests/api/availability.test.js,
    vehicle-scheduling-backend/tests/api/vehicle-maintenance.test.js,
    vehicle-scheduling-backend/tests/api/time-extensions.test.js,
    vehicle-scheduling-backend/tests/api/gps.test.js
  </files>
  <read_first>
    vehicle-scheduling-backend/src/routes/settings.js,
    vehicle-scheduling-backend/src/routes/jobAssignmentRoutes.js,
    vehicle-scheduling-backend/src/routes/jobStatusRoutes.js,
    vehicle-scheduling-backend/src/routes/availabilityRoutes.js,
    vehicle-scheduling-backend/src/routes/vehicle-maintenance.js,
    vehicle-scheduling-backend/src/routes/timeExtension.js,
    vehicle-scheduling-backend/src/routes/gps.js,
    vehicle-scheduling-backend/tests/api/helpers/auth.js,
    vehicle-scheduling-backend/tests/api/helpers/db.mock.js,
    vehicle-scheduling-backend/tests/api/jobs.test.js
  </read_first>
  <action>
    Create test files for the remaining 7 routes following the exact same pattern established in Task 1. Each file MUST:
    - Set `process.env.JWT_SECRET` and `process.env.NODE_ENV = 'test'` at line 1 BEFORE any require
    - Require db.mock before server
    - Test 401 without token, happy path with mocked DB, and permission denied (403) for unauthorized roles

    Route-specific tests:
    a) `settings.test.js`: GET /api/settings (admin 200, technician 403), PUT /api/settings (admin OK)
    b) `job-assignments.test.js`: GET /api/job-assignments (admin 200, no-token 401), POST /api/job-assignments (admin OK, technician 403)
    c) `job-status.test.js`: Read the route file to find exact endpoints. Test status transition endpoints (admin/technician can update status).
    d) `availability.test.js`: GET /api/availability (check for date query params). Test with admin token.
    e) `vehicle-maintenance.test.js`: GET /api/vehicle-maintenance (admin 200), POST /api/vehicle-maintenance (admin OK, technician 403)
    f) `time-extensions.test.js`: GET /api/time-extensions (admin 200, no-token 401), POST /api/time-extensions (technician can create)
    g) `gps.test.js`: Test GPS routes (POST /api/gps/location, GET /api/gps/consent). Read route file for exact paths.

    IMPORTANT: Read each route source file FIRST to determine exact endpoint paths, HTTP methods, and which middleware (verifyToken, requirePermission) is applied. Do not copy paths from other test files.
  </action>
  <verify>
    <automated>cd vehicle-scheduling-backend && npm run test:api 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/settings.test.js exists with at least 2 test cases
    - tests/api/job-assignments.test.js exists with at least 2 test cases
    - tests/api/job-status.test.js exists with at least 2 test cases
    - tests/api/availability.test.js exists with at least 2 test cases
    - tests/api/vehicle-maintenance.test.js exists with at least 2 test cases
    - tests/api/time-extensions.test.js exists with at least 2 test cases
    - tests/api/gps.test.js exists with at least 2 test cases
    - npm run test:api passes with 0 failures across all 14 test files
    - All 15 routes (14 + auth) have at least one test covering them
  </acceptance_criteria>
  <done>All 15 backend route groups have API tests. npm run test:api passes fully. TEST-01 satisfied.</done>
</task>

</tasks>

<verification>
- `cd vehicle-scheduling-backend && npm run test:api` passes with 0 failures
- Every route in src/routes/index.js has a corresponding test file in tests/api/
- Each test file has at least one 401 test and one happy-path test
- No test requires a live MySQL connection
</verification>

<success_criteria>
- All 15 route groups covered by API tests (14 test files + auth)
- npm run test:api passes cleanly
- TEST-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-suite/08-01-SUMMARY.md`
</output>
