---
phase: 08-testing-suite
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - vehicle-scheduling-backend/tests/regression/permissionMatrix.test.js
  - vehicle-scheduling-backend/tests/regression/conflictDetection.test.js
  - vehicle-scheduling-backend/tests/regression/timezoneHandling.test.js
  - vehicle-scheduling-backend/tests/load/concurrent-users.yml
  - vehicle-scheduling-backend/tests/load/generate-token.js
  - vehicle-scheduling-backend/package.json
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "Permission matrix test covers all 4 roles against all protected endpoints"
    - "Conflict detection regression test validates overlapping time windows are caught"
    - "Timezone regression test validates UTC date handling edge cases"
    - "Artillery load test config simulates 20+ concurrent users"
    - "npm run test:regression executes regression tests and passes"
  artifacts:
    - path: "vehicle-scheduling-backend/tests/regression/permissionMatrix.test.js"
      provides: "Role-based access verification for all endpoints"
      min_lines: 80
    - path: "vehicle-scheduling-backend/tests/regression/conflictDetection.test.js"
      provides: "Double-booking and time overlap regression tests"
      min_lines: 40
    - path: "vehicle-scheduling-backend/tests/regression/timezoneHandling.test.js"
      provides: "UTC vs local edge case tests"
      min_lines: 30
    - path: "vehicle-scheduling-backend/tests/load/concurrent-users.yml"
      provides: "Artillery scenario for 20+ concurrent user simulation"
  key_links:
    - from: "tests/regression/permissionMatrix.test.js"
      to: "src/config/constants.js"
      via: "PERMISSIONS map drives test matrix"
      pattern: "makeToken.*admin.*scheduler.*technician"
    - from: "tests/load/concurrent-users.yml"
      to: "http://localhost:3000/api"
      via: "Artillery HTTP requests"
      pattern: "target.*localhost.*3000"
---

<objective>
Create regression test suite (permissions, conflict detection, timezone) and Artillery load test configuration.

Purpose: Satisfy TEST-03 (regression suite), TEST-04 (permission matrix), and TEST-05 (load testing 20+ concurrent users).
Output: 3 regression test files, 1 Artillery YAML config, 1 token generation helper, updated package.json scripts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-testing-suite/08-RESEARCH.md
@.planning/phases/08-testing-suite/08-01-SUMMARY.md

<interfaces>
<!-- Permission map from src/config/constants.js — 22 permission keys across 4 roles -->
PERMISSIONS = {
  'jobs:read': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'jobs:create': [ADMIN, DISPATCHER, SCHEDULER],
  'jobs:update': [ADMIN, DISPATCHER, SCHEDULER],
  'jobs:delete': [ADMIN],
  'jobs:updateStatus': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'assignments:read': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'assignments:create': [ADMIN, DISPATCHER, SCHEDULER],
  'assignments:update': [ADMIN, DISPATCHER, SCHEDULER],
  'assignments:delete': [ADMIN, DISPATCHER, SCHEDULER],
  'vehicles:read': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'vehicles:create': [ADMIN],
  'vehicles:update': [ADMIN],
  'vehicles:delete': [ADMIN],
  'dashboard:read': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'reports:read': [ADMIN, DISPATCHER, SCHEDULER],
  'users:read': [ADMIN],
  'users:create': [ADMIN],
  'users:update': [ADMIN],
  'users:delete': [ADMIN],
  'maintenance:read': [ADMIN, DISPATCHER, SCHEDULER, TECHNICIAN],
  'maintenance:create': [ADMIN],
  'maintenance:update': [ADMIN],
  'maintenance:delete': [ADMIN],
  'settings:read': [ADMIN],
  'settings:update': [ADMIN],
}

<!-- Routes mapped to permissions (extracted from route files) -->
GET /api/jobs → jobs:read
POST /api/jobs → jobs:create
PUT /api/jobs/:id → jobs:update
DELETE /api/jobs/:id → jobs:delete
GET /api/vehicles → vehicles:read
POST /api/vehicles → vehicles:create
GET /api/users → users:read
POST /api/users → users:create
DELETE /api/users/:id → users:delete
GET /api/dashboard/summary → dashboard:read
GET /api/reports → reports:read
GET /api/settings → settings:read
PUT /api/settings → settings:update

<!-- Test helpers from Plan 01 -->
const { makeToken, JWT_SECRET } = require('../api/helpers/auth');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create permission matrix and regression test files</name>
  <files>
    vehicle-scheduling-backend/tests/regression/permissionMatrix.test.js,
    vehicle-scheduling-backend/tests/regression/conflictDetection.test.js,
    vehicle-scheduling-backend/tests/regression/timezoneHandling.test.js
  </files>
  <read_first>
    vehicle-scheduling-backend/src/config/constants.js,
    vehicle-scheduling-backend/src/middleware/auth.js,
    vehicle-scheduling-backend/src/routes/index.js,
    vehicle-scheduling-backend/src/routes/jobs.js,
    vehicle-scheduling-backend/src/routes/vehicles.js,
    vehicle-scheduling-backend/src/routes/users.js,
    vehicle-scheduling-backend/src/routes/settings.js,
    vehicle-scheduling-backend/src/routes/dashboard.js,
    vehicle-scheduling-backend/src/routes/reports.js,
    vehicle-scheduling-backend/src/routes/vehicle-maintenance.js,
    vehicle-scheduling-backend/tests/api/helpers/auth.js,
    vehicle-scheduling-backend/tests/api/helpers/db.mock.js,
    vehicle-scheduling-backend/tests/unit/dateFormatting.test.js
  </read_first>
  <action>
    1. Create `tests/regression/permissionMatrix.test.js`:
       - Set `process.env.JWT_SECRET` and `process.env.NODE_ENV = 'test'` FIRST
       - Require db.mock from `../api/helpers/db.mock` to mock DB before server import
       - Import makeToken from `../api/helpers/auth`
       - Define a MATRIX array: each row is `[description, httpMethod, path, role, allowedStatuses]`
       - Cover ALL permission-gated endpoints for all 4 roles (admin, scheduler, dispatcher, technician)
       - For each permission key in PERMISSIONS, map to the corresponding HTTP endpoint
       - Roles that HAVE the permission: expect status in [200, 201, 400, 404, 500] (not 403)
       - Roles that LACK the permission: expect 403
       - Use `test.each` or `forEach` to iterate the matrix
       - IMPORTANT: Read each route file to confirm which `requirePermission('...')` is used on each endpoint
       - Test both 'scheduler' AND 'dispatcher' roles separately (they should behave identically)
       - Minimum 40 matrix rows covering the 22 permission keys across 4 roles

    2. Create `tests/regression/conflictDetection.test.js`:
       - Set env vars, mock DB, import server
       - Test the availability/conflict detection by mocking DB responses:
         a) Mock DB to return overlapping jobs when checking vehicle availability — verify the route returns conflict error
         b) Mock DB to return no conflicts — verify the route allows assignment
         c) Test time extension impact analysis: mock overlapping subsequent jobs — verify impact is detected
       - Read the actual availability and time extension route files to understand the endpoint logic

    3. Create `tests/regression/timezoneHandling.test.js`:
       - Extend the existing dateFormatting tests with edge cases:
         a) UTC midnight boundary: a job scheduled at 2026-12-31T23:30 UTC should not shift to the next day
         b) Scheduled date string parsing: '2026-01-15' should always resolve to the same date regardless of system TZ
         c) Test that date formatting utilities handle null/undefined gracefully
       - Can be unit-style (no server import needed) if testing utility functions
       - If testing API responses, use the standard mock-DB + server pattern

    PITFALL: The permission matrix test MUST read each route file to know which middleware is applied. Do NOT assume — some routes may use `verifyToken` without `requirePermission` (meaning any authenticated user can access).
  </action>
  <verify>
    <automated>cd vehicle-scheduling-backend && npx jest --testPathPattern=tests/regression --forceExit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/regression/permissionMatrix.test.js exists with at least 40 test cases covering all 4 roles
    - tests/regression/conflictDetection.test.js exists with at least 3 test cases
    - tests/regression/timezoneHandling.test.js exists with at least 3 test cases
    - npm run test:regression passes with 0 failures
    - permissionMatrix.test.js tests both 'scheduler' and 'dispatcher' role strings
  </acceptance_criteria>
  <done>Regression suite covers permission matrix (all roles x all endpoints), conflict detection, and timezone edge cases. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create Artillery load test config and update package.json</name>
  <files>
    vehicle-scheduling-backend/tests/load/concurrent-users.yml,
    vehicle-scheduling-backend/tests/load/generate-token.js,
    vehicle-scheduling-backend/package.json
  </files>
  <read_first>
    vehicle-scheduling-backend/package.json,
    vehicle-scheduling-backend/tests/api/helpers/auth.js,
    vehicle-scheduling-backend/src/routes/index.js
  </read_first>
  <action>
    1. Install Artillery:
       `cd vehicle-scheduling-backend && npm install --save-dev artillery`

    2. Create `tests/load/generate-token.js`:
       - A standalone Node.js script that generates a valid JWT token for load testing
       - Uses jsonwebtoken.sign() with the same secret pattern as the backend
       - Reads JWT_SECRET from env or uses the backend's actual secret
       - Outputs the token to stdout so it can be captured: `node tests/load/generate-token.js`
       - Usage: `TEST_ADMIN_TOKEN=$(node tests/load/generate-token.js) npm run test:load`

    3. Create `tests/load/concurrent-users.yml`:
       - Artillery 2.x YAML config targeting `http://localhost:3000`
       - Config phases:
         - Warm-up: duration 10s, arrivalRate 5
         - Peak load: duration 60s, arrivalRate 20 (20 new virtual users/second = well over 20 concurrent)
       - Use `$processEnvironment.TEST_ADMIN_TOKEN` for auth header
       - Scenarios (weighted):
         a) "Dispatcher reads dashboard" (weight 40): GET /api/dashboard/summary, GET /api/jobs, GET /api/dashboard/stats
         b) "Driver checks assigned jobs" (weight 30): GET /api/jobs, GET /api/notifications
         c) "GPS location update" (weight 20): POST /api/gps/location with lat/lng body
         d) "Create job" (weight 10): POST /api/jobs with minimal valid body
       - Add `expect` assertions: statusCode 200 on GET routes
       - Add `ensure` threshold: p95 response time < 2000ms

    4. Update `package.json` scripts — add:
       - `"test:load": "artillery run tests/load/concurrent-users.yml"`
       - `"test:regression": "jest --testPathPattern=tests/regression --forceExit"` (if not already added in Plan 01)
       - `"test:all": "jest --forceExit"` (if not already added)
       - Add a comment in the load script or a `pretest:load` that echoes: "REQUIRES: live server on port 3000 + TEST_ADMIN_TOKEN env var"

    NOTE: Artillery load tests are NOT part of the automated CI suite — they require a live running server with a seeded database. The test:load script is for manual/staging use.
  </action>
  <verify>
    <automated>cd vehicle-scheduling-backend && npx artillery --version 2>&1 | head -1 && node tests/load/generate-token.js 2>&1 | head -1 && npx artillery validate tests/load/concurrent-users.yml 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - artillery is installed (npx artillery --version succeeds)
    - tests/load/concurrent-users.yml exists and passes `artillery validate`
    - tests/load/generate-token.js outputs a valid JWT string
    - package.json contains "test:load" script referencing artillery
    - package.json contains "test:regression" script
    - Artillery config has arrivalRate of at least 20 in peak phase
    - Artillery config includes at least 3 scenarios with auth headers
  </acceptance_criteria>
  <done>Artillery load test config validated, token generator works, package.json scripts complete. TEST-05 satisfied (config ready for execution against live server).</done>
</task>

</tasks>

<verification>
- `cd vehicle-scheduling-backend && npm run test:regression` passes with 0 failures
- `artillery validate tests/load/concurrent-users.yml` succeeds
- Permission matrix covers all 4 roles across all permission-gated endpoints
- Conflict detection tests mock DB to simulate both overlapping and non-overlapping scenarios
</verification>

<success_criteria>
- TEST-03 satisfied: regression tests cover conflict detection, timezone, permissions
- TEST-04 satisfied: permission matrix tests all 4 roles against all endpoints
- TEST-05 satisfied: Artillery config ready for 20+ concurrent users
- npm run test:regression passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-suite/08-02-SUMMARY.md`
</output>
