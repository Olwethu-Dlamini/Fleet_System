{
  "name": "fleet-scheduler-e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright show-report",
    "capture": "npx playwright test capture-app.spec.js --project=capture",
    "capture:desktop": "npx playwright test capture-app.spec.js --project=capture-desktop",
    "capture:all": "npx playwright test capture-app.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
